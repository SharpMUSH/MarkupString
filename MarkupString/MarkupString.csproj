<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyName>MarkupString</AssemblyName>
    <RootNamespace>MarkupString</RootNamespace>
    <PackageId>MarkupString</PackageId>
    <Title>MarkupString</Title>
    <IsAotCompatible>true</IsAotCompatible>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>CS1591</NoWarn>
    <!-- No PackageValidationBaselineVersion yet: nothing is published, so there is no baseline to
         diff against. The first release sets it, and validation starts catching breaks from then on. -->
    <EnablePackageValidation>true</EnablePackageValidation>
    <Description>Immutable styled text: a string plus layered markup runs, rendered to ANSI, HTML, Pueblo, MXP, BBCode or plain text through a registry of emitters, and round-tripped through JSON.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MinVer" Version="8.0.0" PrivateAssets="all" />
    <PackageReference Include="Microsoft.CodeAnalysis.PublicApiAnalyzers" Version="5.6.0" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="/" />
  </ItemGroup>

  <ItemGroup>
    <!-- MarkupRegistry.AssignOnce: the set-once logic behind Default, which the tests exercise
         against a field of their own because assigning Default would configure the whole test
         process (and RendererTests asserts it is never configured). -->
    <InternalsVisibleTo Include="MarkupString.Tests" />
  </ItemGroup>
</Project>
